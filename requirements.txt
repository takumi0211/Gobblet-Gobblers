# ========================================
# ゴブレットゴブラーズ AI学習プロジェクト
# 最適化版 - 必要なPythonライブラリ
# ========================================

# 深層学習・機械学習フレームワーク
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# 高速化ライブラリ（推奨）
numba>=0.58.0,<1.0.0          # JITコンパイルによる高速化
scipy>=1.10.0,<2.0.0          # 科学計算ライブラリ

# データ可視化
matplotlib>=3.6.0,<4.0.0
seaborn>=0.12.0,<1.0.0        # 統計グラフ用

# 進捗表示・ユーティリティ
tqdm>=4.64.0,<5.0.0
psutil>=5.9.0,<6.0.0          # システム監視用

# Webアプリケーション
Flask>=2.3.0,<3.0.0

# パフォーマンス監視（オプション）
memory-profiler>=0.61.0,<1.0.0

numba>=0.58.0,<1.0.0

# ========================================
# インストールコマンド
# ========================================
# 基本インストール:
# pip install -r requirements.txt
#
# 高速化オプション付き:
# pip install -r requirements.txt
# pip install numba  # さらなる高速化のため
#
# ========================================
# 動作環境
# ========================================
# Python 3.8以上推奨
# CPU: マルチコア推奨（並列処理あり）
# メモリ: 8GB以上推奨（大きなバッチサイズ使用時）
# GPU使用時はCUDA対応のPyTorchを別途インストール
#
# パフォーマンス向上のための最適化:
# - Numba JITコンパイレーション
# - NumPy配列ベースの高速データ構造
# - バッチ処理の最適化
# - 状態ベクトル次元削減 (120→96次元)
# - メモリ効率的なリプレイバッファ 